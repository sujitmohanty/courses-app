<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>
    <%= title %>
  </title>
  <style>
    body {
      font-family: sans-serif;
      padding: 20px;
    }

    nav a {
      margin-right: 15px;
    }
  </style>
</head>
<body>
  <h1>Dashboard</h1>
  <p>Welcome, <%= user.name %>! Your role is: <strong>
        <%= user.role %>
      </strong>.</p>

  <nav>
    <a href="/courses">View All Courses</a>
    <% if (user.role==='instructor' ) { %>
      <a href="/courses/create">Create a New Course</a>
      <% } %>
        <% if (user.role==='student' ) { %>
          <!-- Placeholder for student-specific links -->
          <% } %>
            <a href="/auth/logout">Logout</a>
  </nav>

</body>
</html>